% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lisa.R
\name{lisa_markov}
\alias{lisa_markov}
\title{LISA Markov chain}
\usage{
lisa_markov(data, id, time, value, weights, row_standardize = TRUE)
}
\arguments{
\item{data}{A data frame in long format with one row per unit and period.
The panel must be balanced (every unit present in every period).}

\item{id, time, value}{Character scalars naming the unit, period, and numeric
value columns.}

\item{weights}{A square numeric spatial weights matrix whose row and column
names match the unit ids.}

\item{row_standardize}{Logical; row-standardise \code{weights} before computing
the spatial lag (default \code{TRUE}).}
}
\value{
An object of class \code{sddr_markov}: a 4 by 4 transition matrix over the
quadrants \code{HH}, \code{LH}, \code{LL}, \code{HL}, plus the per-unit-per-period \code{quadrants}
matrix.
}
\description{
A Markov chain over the four quadrants of the Moran scatterplot (Rey, 2001).
Each unit, in each period, is placed in a quadrant according to the sign of
its value (relative to the cross-sectional mean) and the sign of its spatial
lag:
\describe{
\item{\code{HH}}{high value, high neighbourhood (quadrant 1).}
\item{\code{LH}}{low value, high neighbourhood (quadrant 2).}
\item{\code{LL}}{low value, low neighbourhood (quadrant 3).}
\item{\code{HL}}{high value, low neighbourhood (quadrant 4).}
}
The resulting 4 by 4 transition matrix describes how units move between local
spatial-association types over time — capturing the co-evolution of a unit
and its neighbourhood.
}
\examples{
set.seed(1)
n <- 9; periods <- 8
W <- matrix(0, n, n, dimnames = list(1:n, 1:n))
for (i in 1:n) {                       # ring contiguity
  W[i, i \%\% n + 1] <- 1
  W[i, (i - 2) \%\% n + 1] <- 1
}
df <- data.frame(id = rep(1:n, times = periods),
                 time = rep(1:periods, each = n),
                 value = rnorm(n * periods))
lisa_markov(df, "id", "time", "value", weights = W)

}
\references{
Rey, S. J. (2001). Spatial empirics for economic growth and convergence.
\emph{Geographical Analysis}, 33(3), 195-214.
\doi{10.1111/j.1538-4632.2001.tb00444.x}
}
\seealso{
\code{\link[=spatial_markov]{spatial_markov()}}, \code{\link[=markov]{markov()}}
}
