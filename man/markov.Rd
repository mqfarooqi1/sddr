% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markov.R
\name{markov}
\alias{markov}
\title{Classic Markov chain for distribution dynamics}
\usage{
markov(data, id, time, value, k = 5, breaks = NULL, fixed = TRUE)
}
\arguments{
\item{data}{A data frame in long format with one row per unit and period.}

\item{id}{Name (character scalar) of the column identifying the unit.}

\item{time}{Name (character scalar) of the column giving the period. Periods
are compared in sorted order within each unit.}

\item{value}{Name (character scalar) of the numeric column to analyse.}

\item{k}{Integer number of classes to discretise \code{value} into. Default \code{5}
(quintiles). Ignored if \code{breaks} is supplied.}

\item{breaks}{Optional numeric vector of \emph{interior} class boundaries. If
supplied these fixed cut points are used instead of data-driven quantiles,
giving \code{length(breaks) + 1} classes.}

\item{fixed}{Logical. If \code{TRUE} (default) a single set of quantile breaks is
estimated from the pooled distribution and applied to every period
(absolute mobility). If \code{FALSE}, breaks are re-estimated within each period
(relative mobility). Ignored when \code{breaks} is supplied.}
}
\value{
An object of class \code{sddr_markov}: a list with elements
\describe{
\item{\code{matrix}}{The \code{k} by \code{k} row-stochastic transition probability
matrix \code{P}.}
\item{\code{transitions}}{The \code{k} by \code{k} matrix of observed transition counts.}
\item{\code{steady_state}}{The ergodic (stationary) distribution of \code{P}.}
\item{\code{classes}}{Number of classes \code{k}.}
\item{\code{breaks}}{The class boundaries used (when \code{fixed}).}
\item{\code{n}}{Number of units contributing at least one transition.}
\item{\code{n_transitions}}{Total number of observed transitions.}
\item{\code{data}}{The input data augmented with the integer \code{state} column.}
}
}
\description{
Discretises a continuous longitudinal variable into ordered classes and
estimates a discrete-time, first-order Markov transition probability matrix
together with its ergodic (steady-state) distribution. This is the classic
distribution-dynamics estimator of Quah (1993): the transition matrix
summarises how units move between parts of the distribution from one period
to the next, and the steady-state distribution describes where the process
settles in the long run.

Input is \emph{tidy}, long-format panel data with one row per unit-period. The
estimator is agnostic to what the units are (regions, firms, households,
\dots) and to whether space is involved; spatial conditioning is added by a
companion \code{spatial_markov()} function (available in a later release).
}
\examples{
set.seed(1)
df <- data.frame(
  id = rep(1:50, each = 4),
  time = rep(2000:2003, times = 50),
  value = rnorm(200)
)
m <- markov(df, id = "id", time = "time", value = "value", k = 4)
m
m$steady_state

}
\references{
Quah, D. (1993). Empirical cross-section dynamics in economic growth.
\emph{European Economic Review}, 37(2-3), 426-434.
}
\seealso{
\code{\link[=steady_state]{steady_state()}}
}
