% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mobility.R
\name{mobility}
\alias{mobility}
\title{Markov mobility indices}
\usage{
mobility(x, measure = "all", ini = NULL)
}
\arguments{
\item{x}{A row-stochastic matrix, or an object of class \code{sddr_markov}.}

\item{measure}{One of \code{"prais"}, \code{"determinant"}, \code{"L2"}, \code{"shorrock1"},
\code{"shorrock2"}, or \code{"all"} (default) to return every index.}

\item{ini}{Optional initial distribution (length \code{k}) for the Shorrock
indices; defaults to uniform.}
}
\value{
A named numeric vector of the requested index or indices.
}
\description{
Scalar summaries of how much mixing a transition matrix implies — larger
values mean more mobility (a faster-churning distribution), values near zero
mean a near-immobile system. Several classical indices are provided:

\describe{
\item{\code{prais}}{Prais index \eqn{(k - \mathrm{tr}(P)) / (k - 1)} — average
probability of leaving a class.}
\item{\code{determinant}}{\eqn{1 - |\det(P)|}.}
\item{\code{L2}}{\eqn{1 - |\lambda_2|}, one minus the second-largest eigenvalue
modulus (the mixing rate).}
\item{\code{shorrock1}}{\eqn{(k - k \sum_i \pi_i p_{ii}) / (k - 1)}, weighted by
an initial distribution \code{ini}.}
\item{\code{shorrock2}}{\eqn{\sum_i \sum_j \pi_i p_{ij} |i - j| / (k - 1)},
penalising longer jumps.}
}
}
\examples{
P <- matrix(c(0.7, 0.2, 0.1,
              0.2, 0.6, 0.2,
              0.1, 0.3, 0.6), nrow = 3, byrow = TRUE)
mobility(P)
mobility(P, "prais")

}
\references{
Prais, S. J. (1955). Measuring social mobility. \emph{JRSS A}, 118(1), 56-66.
Shorrocks, A. F. (1978). The measurement of mobility. \emph{Econometrica},
46(5), 1013-1024. \doi{10.2307/1911433}
}
