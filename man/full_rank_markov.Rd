% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank-markov.R
\name{full_rank_markov}
\alias{full_rank_markov}
\title{Full-rank Markov chain}
\usage{
full_rank_markov(data, id, time, value)
}
\arguments{
\item{data}{A data frame in long format with one row per unit and period.
The panel must be balanced (every unit present in every period).}

\item{id, time, value}{Character scalars naming the unit, period, and numeric
value columns.}
}
\value{
An object of class \code{sddr_markov} (an \code{n} by \code{n} rank transition
matrix, its counts, and ergodic distribution).
}
\description{
A Markov chain over \emph{ranks} rather than discretised classes: each unit's
rank within the cross-section (1 = highest value, \code{n} = lowest) becomes its
state, and the \code{n} by \code{n} matrix records how ranks transition from one
period to the next (Rey, 2014). Unlike \code{\link[=markov]{markov()}} no binning is needed — the
full rank ordering is retained.
}
\examples{
set.seed(1)
df <- data.frame(
  id = rep(1:6, times = 8),
  time = rep(1:8, each = 6),
  value = rnorm(48)
)
full_rank_markov(df, "id", "time", "value")

}
\references{
Rey, S. J. (2014). Fast algorithms for a space-time concordance measure.
\emph{Computational Statistics}, 29(3-4), 799-811.
}
\seealso{
\code{\link[=markov]{markov()}}, \code{\link[=geo_rank_markov]{geo_rank_markov()}}
}
