% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank-markov.R
\name{geo_rank_markov}
\alias{geo_rank_markov}
\title{Geographic-rank Markov chain}
\usage{
geo_rank_markov(data, id, time, value)
}
\arguments{
\item{data}{A data frame in long format with one row per unit and period.
The panel must be balanced (every unit present in every period).}

\item{id, time, value}{Character scalars naming the unit, period, and numeric
value columns.}
}
\value{
An object of class \code{sddr_markov} (an \code{n} by \code{n} transition matrix
over units, its counts, and ergodic distribution).
}
\description{
A Markov chain that tracks, for each rank position, \emph{which unit} occupies it
over time (Rey, 2016). States are the geographic units, so the \code{n} by \code{n}
matrix records how units exchange rank positions — a spatially explicit view
of positional mobility.
}
\examples{
set.seed(1)
df <- data.frame(
  id = rep(1:6, times = 8),
  time = rep(1:8, each = 6),
  value = rnorm(48)
)
geo_rank_markov(df, "id", "time", "value")

}
\references{
Rey, S. J. (2016). Space-time patterns of rank concordance: Local indicators
of mobility association. \emph{Annals of the AAG}, 106(4), 788-803.
}
\seealso{
\code{\link[=full_rank_markov]{full_rank_markov()}}, \code{\link[=markov]{markov()}}
}
