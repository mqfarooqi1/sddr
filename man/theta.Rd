% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank.R
\name{theta}
\alias{theta}
\title{Theta rank-mobility statistic}
\usage{
theta(y, regime)
}
\arguments{
\item{y}{A numeric matrix of values, units in rows and time periods in
columns (at least two columns).}

\item{regime}{A vector of length \code{nrow(y)} assigning each unit to a regime.}
}
\value{
An object of class \code{sddr_theta}: a list with \code{theta} (one value per
period-to-period transition), the per-period \code{total} absolute rank change,
and the column rank matrix \code{ranks}.
}
\description{
The Theta statistic (Rey, 2004) measures how much of the overall rank
movement in a system is \emph{within-regime} exchange. For each period-to-period
transition it is the sum over regimes of the absolute \emph{net} within-regime
rank change, divided by the total absolute rank change. Values near zero
indicate that rank movement is dominated by within-regime shuffling that
nets out; larger values indicate coherent regime-level movement.
}
\examples{
set.seed(1)
y <- matrix(rnorm(40), nrow = 10)    # 10 units, 4 periods
regime <- rep(c("a", "b"), each = 5)
theta(y, regime)

}
\references{
Rey, S. J. (2004). Spatial dependence in the evolution of regional income
distributions. In \emph{Spatial Econometrics and Spatial Statistics}, 194-213.
}
\seealso{
\code{\link[=tau]{tau()}}
}
