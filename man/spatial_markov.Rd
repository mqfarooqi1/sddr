% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-markov.R
\name{spatial_markov}
\alias{spatial_markov}
\title{Spatial Markov chain for distribution dynamics}
\usage{
spatial_markov(
  data,
  id,
  time,
  value,
  weights = NULL,
  lag = NULL,
  k = 5,
  m = k,
  breaks = NULL,
  fixed = TRUE,
  row_standardize = TRUE
)
}
\arguments{
\item{data}{A data frame in long format with one row per unit and period.}

\item{id}{Name (character scalar) of the column identifying the unit.}

\item{time}{Name (character scalar) of the column giving the period. Periods
are compared in sorted order within each unit.}

\item{value}{Name (character scalar) of the numeric column to analyse.}

\item{weights}{Optional square numeric spatial weights matrix whose row and
column names match the unit ids. Used to compute the spatial lag of \code{value}
within each period. Requires a balanced panel. Exactly one of \code{weights} or
\code{lag} must be supplied.}

\item{lag}{Optional name (character scalar) of a column giving a precomputed
spatial lag of \code{value}.}

\item{k}{Integer number of classes to discretise \code{value} into. Default \code{5}
(quintiles). Ignored if \code{breaks} is supplied.}

\item{m}{Integer number of spatial-lag classes to condition on. Defaults to
\code{k}.}

\item{breaks}{Optional numeric vector of \emph{interior} class boundaries. If
supplied these fixed cut points are used instead of data-driven quantiles,
giving \code{length(breaks) + 1} classes.}

\item{fixed}{Logical. If \code{TRUE} (default) a single set of quantile breaks is
estimated from the pooled distribution and applied to every period
(absolute mobility). If \code{FALSE}, breaks are re-estimated within each period
(relative mobility). Ignored when \code{breaks} is supplied.}

\item{row_standardize}{Logical; row-standardise \code{weights} before computing
the lag (default \code{TRUE}). Ignored when \code{lag} is supplied.}
}
\value{
An object of class \code{sddr_spatial_markov}: a list with elements
\describe{
\item{\code{matrices}}{Length-\code{m} list of \code{k} by \code{k} conditional transition
probability matrices, one per spatial-lag class.}
\item{\code{transitions}}{Length-\code{m} list of conditional count matrices.}
\item{\code{pooled}}{The \code{k} by \code{k} pooled (aspatial) transition matrix; this
equals the matrix from \code{\link[=markov]{markov()}} with the same class definition.}
\item{\code{pooled_transitions}}{Pooled count matrix.}
\item{\code{steady_states}}{Length-\code{m} list of conditional ergodic
distributions (\code{NA} where a conditional matrix has unvisited classes).}
\item{\code{classes}, \code{lag_classes}}{\code{k} and \code{m}.}
\item{\code{n}, \code{n_transitions}}{Units contributing, and total transitions.}
\item{\code{data}}{Input augmented with \code{lagval}, value state and lag state.}
}
}
\description{
Estimates a spatial Markov chain (Rey, 2001): a set of class-transition
probability matrices, each conditioned on the \emph{spatial context} of a unit at
the start of the period. Where \code{\link[=markov]{markov()}} asks "given a unit's own class,
where does it move next?", \code{spatial_markov()} asks "given a unit's own class
\strong{and the class of its spatial neighbourhood}, where does it move next?".
Comparing the conditional matrices with the pooled (aspatial) matrix reveals
whether, and how, spatial context shapes distribution dynamics.

The neighbourhood is summarised by the \emph{spatial lag}: a (row-standardised)
weighted average of neighbours' values. Supply either a spatial weights
matrix via \code{weights} (the lag is computed for you, per period), or a
precomputed spatial-lag column via \code{lag} (keeping the estimator independent
of any particular spatial-data stack).
}
\examples{
set.seed(1)
n <- 12; periods <- 8
# Ring contiguity: each unit neighbours the next and previous on a circle.
W <- matrix(0, n, n, dimnames = list(1:n, 1:n))
for (i in 1:n) {
  W[i, i \%\% n + 1] <- 1
  W[i, (i - 2) \%\% n + 1] <- 1
}
df <- do.call(rbind, lapply(seq_len(periods), function(p)
  data.frame(id = 1:n, time = p, value = rnorm(n))))
sm <- spatial_markov(df, "id", "time", "value", weights = W, k = 3)
sm

}
\references{
Rey, S. J. (2001). Spatial empirics for economic growth and convergence.
\emph{Geographical Analysis}, 33(3), 195-214.
\doi{10.1111/j.1538-4632.2001.tb00444.x}
}
\seealso{
\code{\link[=markov]{markov()}}
}
